[tool.ruff]
line-length = 120
target-version = "py310"
select = ["E", "F", "W", "I", "N"]
exclude = [
  "**/migrations/**",
  "**/__pycache__/**",
  ".venv",
  "venv",
  "build",
  "dist",
]

[tool.ruff.format]
indent-style = "space"
line-ending = "lf"
